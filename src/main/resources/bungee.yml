# project meta data for BungeeCord
name: ${project.name}
# ${-} will be automatically replaced by Maven
main: ${project.groupId}.${project.artifactId}.${project.name}

version: ${project.version}
author: games647, https://github.com/games647/PlayerScan/graphs/contributors

description: |
    ${project.description}
